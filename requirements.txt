pandas>=2.2.0
numpy>=1.26.0
yfinance>=0.2.54
plotly>=5.24.0
streamlit>=1.40.0
duckdb>=1.1.0
pyarrow>=16.0.0
scikit-learn>=1.5.0
xgboost>=2.1.0
joblib>=1.4.0
python-dotenv>=1.0.1
requests>=2.32.0
tabulate>=0.9.0
imbalanced-learn>=0.12.0
# Optional — used only by feature_transformation_experiments.py (Phase 8).
# The script falls back gracefully if LightGBM is not installed, so the
# main pipeline still runs without it.
lightgbm>=4.3.0
